* [ ] `lark-cli --version` 返回版本号。

* [ ] `lark-cli auth login` 正确保存凭证。

* [ ] `lark-cli message send` 能向测试用户/群组发送消息。

* [ ] `lark-cli doc create` 能创建文档并返回 URL。

* [ ] `lark-cli base record list` 能以 JSON 格式返回记录。

* [ ] `LARK_SKILL.md` 存在且准确描述了 CLI 命令。

